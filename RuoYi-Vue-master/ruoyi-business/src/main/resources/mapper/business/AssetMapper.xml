<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.business.mapper.AssetMapper">
    <select id="getHomeAssetStats" resultType="com.ruoyi.business.domain.model.HomeAssetStatsVO">
        SELECT COUNT(1)                                                                      AS collectionCount,
               COUNT(CASE WHEN original_code IS NOT NULL AND original_code != '' THEN 1 END) AS matchCount
        FROM asset
        where delete_flag = 0
          and project_id = #{projectId}
    </select>
</mapper>